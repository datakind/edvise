# Edvise (ES) inference: data_ingestion -> es_data_audit -> es_feature_generation;
# downstream tasks match github_pdp_inference.yml.
resources:
  jobs:
    github_sourced_es_inference_pipeline:
      name: github_sourced_es_inference_pipeline
      webhook_notifications:
        on_failure:
          - id: ${var.inference_slack_webhook_id}
      max_concurrent_runs: 5
      tasks:
        - task_key: data_ingestion
          spark_python_task:
            python_file: src/edvise/scripts/pdp_gcp_databricks_ingestion.py
            source: GIT
            parameters:
              - --cohort_file_name
              - "{{job.parameters.cohort_file_name}}"
              - --course_file_name
              - "{{job.parameters.course_file_name}}"
              - --databricks_institution_name
              - "{{job.parameters.databricks_institution_name}}"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
              - --DB_workspace
              - "{{job.parameters.DB_workspace}}"
              - --gcp_bucket_name
              - "{{job.parameters.gcp_bucket_name}}"
              - --model_name
              - "{{job.parameters.model_name}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: google-cloud-storage==2.19.0
        - task_key: data_audit
          depends_on:
            - task_key: data_ingestion
          spark_python_task:
            python_file: src/edvise/scripts/es_data_audit.py
            source: GIT
            parameters:
              - --bronze_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_bronze/bronze_volume"
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --DB_workspace
              - "{{job.parameters.DB_workspace}}"
              - --cohort_dataset_validated_path
              - "{{tasks.data_ingestion.values.cohort_dataset_validated_path}}"
              - --course_dataset_validated_path
              - "{{tasks.data_ingestion.values.course_dataset_validated_path}}"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --job_type
              - "{{job.parameters.job_type}}"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: scikit-learn==1.5.2
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
        - task_key: feature_generation
          depends_on:
            - task_key: data_audit
          spark_python_task:
            python_file: src/edvise/scripts/es_feature_generation.py
            source: GIT
            parameters:
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --job_type
              - "{{job.parameters.job_type}}"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: scikit-learn==1.5.2
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
        - task_key: checkpoint
          depends_on:
            - task_key: feature_generation
          spark_python_task:
            python_file: src/edvise/scripts/checkpoints.py
            source: GIT
            parameters:
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --schema_type
              - "{{job.parameters.schema_type}}"
              - --job_type
              - "{{job.parameters.job_type}}"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: scikit-learn==1.5.2
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
        - task_key: student_selection
          depends_on:
            - task_key: feature_generation
          spark_python_task:
            python_file: src/edvise/scripts/student_selection.py
            source: GIT
            parameters:
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --schema_type
              - "{{job.parameters.schema_type}}"
              - --job_type
              - "{{job.parameters.job_type}}"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: scikit-learn==1.5.2
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
        - task_key: inf_prep
          depends_on:
            - task_key: checkpoint
            - task_key: student_selection
          spark_python_task:
            python_file: src/edvise/scripts/inf_prep.py
            source: GIT
            parameters:
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
              - --term_filter
              - "{{job.parameters.term_filter}}"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --schema_type
              - "{{job.parameters.schema_type}}"
              - --job_type
              - "{{job.parameters.job_type}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
        - task_key: inference_h2o
          depends_on:
            - task_key: inf_prep
          spark_python_task:
            python_file: src/edvise/scripts/inference_h2o.py
            source: GIT
            parameters:
              - --silver_volume_path
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_silver/silver_volume"
              - --db_run_id
              - "{{job.parameters.db_run_id}}"
              - --ds_run_as
              - "{{job.parameters.ds_run_as}}"
              - --DB_workspace
              - "{{job.parameters.DB_workspace}}"
              - --databricks_institution_name
              - "{{job.parameters.databricks_institution_name}}"
              - --datakind_notification_email
              - "{{job.parameters.datakind_notification_email}}"
              - --DK_CC_EMAIL
              - "{{job.parameters.DK_CC_EMAIL}}"
              - --job_root_dir  
              - "/Volumes/{{job.parameters.DB_workspace}}/{{job.parameters.databricks_institution_name}}_gold/gold_volume/inference_jobs/{{job.parameters.db_run_id}}"
              - --config_file_path
              - "{{tasks.data_ingestion.values.config_file_path}}"
              - --schema_type
              - "{{job.parameters.schema_type}}"
          job_cluster_key: edvise-pdp-inference-pipeline-cluster
          libraries:
            - pypi:
                package: numpy==1.26.4
            - pypi:
                package: pandas==2.2.3
            - pypi:
                package: faker==33.1.0
            - pypi:
                package: google-cloud-storage==2.19.0
            - pypi:
                package: missingno==0.5.2
            - pypi:
                package: tomli==2.2.1
            - pypi:
                package: tomlkit==0.13.3
            - pypi:
                package: pandera==0.23.0
            - pypi:
                package: pydantic~=2.10
            - pypi:
                package: h2o==3.46.0.7
            - pypi:
                package: shap~=0.46.0
            - pypi:
                package: mlflow~=2.22
            - pypi:
                package: scikit-learn==1.5.2      
      job_clusters:
        - job_cluster_key: edvise-pdp-inference-pipeline-cluster
          new_cluster:
            cluster_name: ""
            spark_version: 15.4.x-cpu-ml-scala2.12
            spark_conf:
              spark.master: local[*, 4]
              spark.databricks.cluster.profile: singleNode
            gcp_attributes:
              use_preemptible_executors: false
              availability: ON_DEMAND_GCP
              zone_id: HA
            node_type_id: n2-standard-16
            custom_tags:
              ResourceClass: SingleNode
              x-databricks-nextgen-cluster: "true"
            enable_elastic_disk: true
            data_security_mode: SINGLE_USER
            runtime_engine: STANDARD
            num_workers: 0
      queue:
        enabled: true
      parameters:
        - name: term_filter
          default: ""
        - name: schema_type
          default: edvise
        - name: cohort_file_name
          default: 2025-09-19_CCC Student File De-Duplicated.csv
        - name: course_file_name
          default: 2025-09-19_CCC Course File.csv
        - name: databricks_institution_name
          default: city_cols_of_chicago
        - name: db_run_id
          default: "{{job.run_id}}"
        - name: DB_workspace
          default: ${var.DB_workspace}
        - name: DK_CC_EMAIL
          default: ${var.datakind_notification_email}
        - name: config_file_name
          default: ${var.config_file_name}
        - name: ds_run_as
          default: ${var.ds_run_as}
        - name: gcp_bucket_name
          default: databricks-2052166062819251-unitycatalog
        - name: datakind_notification_email
          default: ${var.datakind_notification_email}
        - name: service_account_executer
          default: ${var.service_account_executer}
        - name: job_type
          default: inference
        - name: model_name
          default: retention_into_year_2_all_degrees
      permissions:
        - group_name: ${var.datakind_group_to_manage_workflow} 
          level: CAN_MANAGE
        - service_principal_name: ${var.ds_run_as}
          level: CAN_MANAGE
        - user_name: ${var.service_account_executer}
          level: CAN_MANAGE