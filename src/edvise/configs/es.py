"""ES (Enrollment/School) project config. Placeholder using PDP structure until ES-specific config is implemented."""

from edvise.configs.pdp import InferenceConfig, PDPProjectConfig

# Alias for ES projects; customize when ES-specific config is needed
ESProjectConfig = PDPProjectConfig
